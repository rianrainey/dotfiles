{
  "$schema": "https://opencode.ai/tui.json",
  // Sidebar starts open by default in v1.18.4.
  // Toggle it closed once with ctrl+x + b and it persists across restarts.
  // These sidebar config keys are proposed upstream but not yet supported:
  // "sidebar": {
  //   "visible": false
  // }
}
