# Search project roots first. The ":2" suffix lets tmux-sessionizer find
# worktrees one level below repo group directories without crawling too deeply.
TS_SEARCH_PATHS=(
  "$HOME/Documents/code:2"
  "$HOME/.config:1"
)
