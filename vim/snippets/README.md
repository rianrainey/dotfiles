These are custom snippets in addition to the ones provide by the
Snippets from Janus.

### Configuration
This parent directory is symlinked in `~/.janus/mysnippets/snippets`

### Troubleshooting
If this breaks, consider adding the `_.snippets` file.
